name: gari_api
description: GariGo HTTP + Socket client
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.12.1
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.8.0+1
  socket_io_client: ^2.0.3+1

dev_dependencies:
  flutter_lints: ^6.0.0
