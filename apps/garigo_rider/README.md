# GariGo Rider

```bash
cd apps/garigo_rider
flutter pub get
flutter run
```

**Demo:** Language → phone any → OTP `123456` (or Continue as Guest) → Where to? → pick Megenagna → Bajaj → Telebirr → Request → Demo match → PIN `4821` → trip → rate.
